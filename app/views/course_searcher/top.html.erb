<!--
  html風の記述でうまくサイトを構成します
  テンプレートはtraining_task/app/views/layouts/application.html.erb
-->

<h1>CourseSearcher</h1>
<p>logo image</p>
<%= link_to "All_search", {controller: 'course_searcher', action: 'multiple_list'}, {method: :get} %><br>
word search

<!--
  なんとformを使うためにそれ用のタグがあるらしいな？
  というわけでform_withを使って検索窓、ラジオボタンを実装する
  url:指定しているのはmodel:でうまくurlを発行できない？ようだ、modelと接続できていないかもしれない
-->
<%= form_with url: course_searcher_multiple_list_path(@courses), :method => 'get', local: true do |form| %>
  <%= form.text_field :search %>
  <%= form.radio_button :mode, 'keyword', {:checked => true} %>keyword
  <%= form.radio_button :mode, 'category', {:checked => false} %>category<br>
  <%= form.submit 'Search', :name => nil %>
<% end %>

